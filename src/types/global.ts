export type ProductThumbnail = {
  id: string;
  name: string;
  price: number;
  image: string;
};

export type ProductPreview = {
  id: string;
  name: string;
  price: number;
  image: string;
  description: string;
};
